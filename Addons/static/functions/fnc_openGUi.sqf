// #include "..\script_component.hpp"

// params  
// private _isSpawner = GET_TEXT (configFile >> "CfgVehicles" >> (_vehicle) >> QGVAR(_isSpawner));
// if (QGVAR(_isSpawner) = "") exitWith {hint "No Spawner registered to this object"};

// if (_isSpawner isEqualTo QEGVAR(_isSpawner,dialog)) exitWith {
// 	call EFUNC(openDialog);
// };

// createDialog [_isSpawner, true];